{
    "name": "fullstack-app",
    "type": "module",
    "scripts": {
        "build": "npm install && cd frontend && npm install && npm run build:prod && cd ../backend && npm install && npm run build && cd ../",
        "start": "cd backend && npm run build && cd .. && node server.js" 
    },
    "dependencies": {
        "express": "^4.19.2",
        "next": "^16.0.0",
        "dotenv": "^17.2.3"
    },
    "engines": {
        "node": ">=18"
    }
}